import React, { Component } from 'react';
import Section from '../Composition/EventCounter/Section';

class Context extends Component {
    render() {
        return (
            <div>
                <h2>Context api</h2>
                <Section />
            </div>
        );
    }
}

export default Context;