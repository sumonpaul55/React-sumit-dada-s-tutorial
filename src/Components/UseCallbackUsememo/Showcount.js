import React from 'react';

const Showcount = ({title, count}) => {
    
    return (
        <div>
            <h2>{title} is {count}</h2>
           
        </div>
    );
};

export default Showcount;